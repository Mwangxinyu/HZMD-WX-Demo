<card-panel title="{{ title }}" bottom-num="{{ bottomNum }}" bg="companyResumeBg" titleCenter>
    <view class="content" slot="content">
        <text wx:for="{{ info }}" wx:key="{{ index }}">{{ item }}</text>
    </view>
</card-panel>